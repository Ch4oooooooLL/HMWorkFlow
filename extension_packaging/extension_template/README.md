# HM WorkFlow Extension Template

This template is regenerated by `extension_packaging/pack_hmworkflow_extension.py`.

Build from the project root:

```bat
extension_packaging\build_extension.bat
```

The distributable extension folder and zip package are written to `dist/`.
